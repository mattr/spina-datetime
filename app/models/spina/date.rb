module Spina
  # Date stores just that -- the date entered via the datepicker. Use 
  # Spina::DateTime if you need to store a time component as well.
  class Date < ::Spina::ApplicationRecord
  end
end
